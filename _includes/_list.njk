{%- for post in posts -%}
    <article>
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        <p>{{ post.inputPath }} | {{ post.date }}</p>
        <p>{{ post.data.summary }}</p>
    </article>
{%- endfor -%}

{% include "_pagination.njk" %}